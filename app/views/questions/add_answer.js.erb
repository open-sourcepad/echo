$('.answers-list').append("<%= j render partial: 'answer_fields', locals: {b: @answer} %>")

if ($('.answer-group').length >= 3) {
  $('.answer-button').addClass('hidden')
}

$('.upload-image').on('click', function(e) {
  e.preventDefault();
  return $(this).next('input[type=file]').click();
});
